generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Minimal schema for Phase 0 bootstrap.
// Full schema (User, Game, Match, Player, Score) will be added in Phase 1.
